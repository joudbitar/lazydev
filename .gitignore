# Your local registry — machine-specific paths and project names.
# Generated by `lazydev scan`; start from projects.example.json.
projects.json

# Control-plane capability token — a per-machine secret the daemon mints at boot.
control-token

# Runtime state — regenerated by the daemon, never committed.
logs/
*.log
daemon.out
daemon.err

# Caddyfile backups the installer drops.
*.bak
*.bak.*

.DS_Store

# Claude Code worktrees / workflow scratch
.claude/
